const port = 4000
const serverurl = 'http://localhost:' + port
export const requests = {

   postapi: serverurl + '/post',
   adoptantapi: serverurl + '/users',
   especeapi: serverurl + '/especes',
   demandeapi: serverurl + '/demandes',
   symptomeapi: serverurl + '/symptome',

}
